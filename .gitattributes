# 确保 shell 脚本在 Windows 上保持 LF（避免 CRLF 导致 bash 执行失败）
*.sh text eol=lf

# PowerShell 脚本保持 CRLF（Windows PowerShell 5.1 here-string / 解析要求）
# 注意：.gitattributes 只能强制 CRLF；UTF-8 BOM 需编辑时保持（PS 5.1 无 BOM 会中文乱码、脚本崩）
*.ps1 text eol=crlf
